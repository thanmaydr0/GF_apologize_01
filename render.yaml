services:
  - type: static
    name: romantic-reconciliation
    env: static
    buildCommand: npm run build
    publishDir: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
